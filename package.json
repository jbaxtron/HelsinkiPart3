{
  "name": "phonebookbackend",
  "version": "1.0.0",
  "description": "backendforPhonebookProject",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "\"echo \"Error: no test specified\" && exit 1\"",
    "build:ui": "rm -rf build && cd ../thephonebook/ && npm run build --prod && cp -r build ../phonebookBackend",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",    
    "logs:prod": "heroku logs --tail"
  },
  "author": "Jesse Baxtron",
  "license": "UNLICENSED",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
